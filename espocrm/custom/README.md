# EspoCRM Custom Configuration

Copy your EspoCRM custom files here before deployment:

```
custom/
└── Espo/
    └── Custom/
        └── Resources/
            ├── metadata/
            ├── layouts/
            └── i18n/
```

Custom files will be automatically loaded by EspoCRM container.
